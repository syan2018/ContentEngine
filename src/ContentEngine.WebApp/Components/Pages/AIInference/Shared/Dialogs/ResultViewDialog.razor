@using MudBlazor
@using LiteDB
@inject IJSRuntime JSRuntime
@inject ISnackbar Snackbar

<link href="Shared/AIInferenceStyles.css" rel="stylesheet" />

<MudDialog>
    <DialogContent>
        <MudContainer Class="pa-0">
            <MudStack Spacing="4">
                <!-- 头部信息 -->
                <div>
                    <MudText Typo="Typo.h6" Class="mb-2">AI生成结果</MudText>
                    <MudText Typo="Typo.body2" Color="Color.Secondary">
                        组合ID: <code class="combination-id">@CombinationId</code>
                    </MudText>
                </div>

                <MudDivider />

                <!-- 输入数据 -->
                @if (InputData?.Any() == true)
                {
                    <div>
                        <MudText Typo="Typo.subtitle1" Class="mb-2 d-flex align-center">
                            <MudIcon Icon="@Icons.Material.Filled.Input" Class="mr-2" Color="Color.Info" />
                            输入数据
                        </MudText>
                        
                        <MudGrid Spacing="2">
                            @foreach (var kvp in InputData)
                            {
                                <MudItem xs="12" sm="6" md="4">
                                    <MudCard Outlined="true" Class="pa-2">
                                        <MudText Typo="Typo.caption" Color="Color.Secondary">@kvp.Key</MudText>
                                        <MudText Typo="Typo.body2" Class="font-weight-medium">
                                            @(kvp.Value?.ToString() ?? "null")
                                        </MudText>
                                    </MudCard>
                                </MudItem>
                            }
                        </MudGrid>
                    </div>
                }

                <!-- 生成内容 -->
                <div>
                    <div class="d-flex justify-space-between align-center mb-2">
                        <MudText Typo="Typo.subtitle1" Class="d-flex align-center">
                            <MudIcon Icon="@Icons.Material.Filled.AutoAwesome" Class="mr-2" Color="Color.Primary" />
                            生成内容
                        </MudText>
                        <MudIconButton Icon="@Icons.Material.Filled.ContentCopy" 
                                      OnClick="CopyContent" 
                                      Size="Size.Small"
                                      Title="复制内容" />
                    </div>
                    
                    <MudTabs Dense="true">
                        @if (HasStructuredData && StructuredFields?.Any() == true)
                        {
                            <MudTabPanel Text="结构化数据" Icon="@Icons.Material.Filled.TableChart">
                                <MudPaper Class="pa-4" Outlined="true">
                                    <MudSimpleTable Dense="true" Striped="true" Bordered="true">
                                        <thead>
                                            <tr>
                                                <th style="font-weight: bold;">字段名</th>
                                                <th style="font-weight: bold;">值</th>
                                                <th style="font-weight: bold;">类型</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @foreach (var field in StructuredFields.OrderBy(kvp => kvp.Key))
                                            {
                                                <tr>
                                                    <td style="font-family: monospace; font-weight: bold;">@field.Key</td>
                                                    <td style="max-width: 400px; word-break: break-word;">
                                                        @if (field.Value != null)
                                                        {
                                                            @field.Value.ToString()
                                                        }
                                                        else
                                                        {
                                                            <span style="color: gray; font-style: italic;">null</span>
                                                        }
                                                    </td>
                                                    <td style="font-size: 0.8em; color: gray;">
                                                        @(field.Value?.GetType().Name ?? "null")
                                                    </td>
                                                </tr>
                                            }
                                        </tbody>
                                    </MudSimpleTable>
                                </MudPaper>
                            </MudTabPanel>
                        }
                        
                        <MudTabPanel Text="文本">
                            <MudPaper Class="pa-4 result-content" Outlined="true">
                                <MudText Typo="Typo.body2" Class="result-text" Style="white-space: pre-wrap; line-height: 1.6;">
                                    @Content
                                </MudText>
                            </MudPaper>
                        </MudTabPanel>
                        
                        <MudTabPanel Text="JSON">
                            <MudPaper Class="pa-4 result-content" Outlined="true">
                                @if (HasStructuredData && StructuredFields?.Any() == true)
                                {
                                    <MudText Typo="Typo.subtitle2" Class="mb-2">解析后的结构化数据：</MudText>
                                    <pre style="background: #f8f9fa; padding: 12px; border-radius: 4px;">@System.Text.Json.JsonSerializer.Serialize(StructuredFields, new System.Text.Json.JsonSerializerOptions { WriteIndented = true, Encoder = System.Text.Encodings.Web.JavaScriptEncoder.UnsafeRelaxedJsonEscaping })</pre>
                                    
                                    <MudDivider Class="my-3" />
                                    <MudText Typo="Typo.subtitle2" Class="mb-2">原始AI输出：</MudText>
                                }
                                
                                @if (TryFormatJson(Content, out var prettyJson))
                                {
                                    <pre>@prettyJson</pre>
                                }
                                else
                                {
                                    <MudText Typo="Typo.body2" Color="Color.Secondary">非合法 JSON，显示为原始文本。</MudText>
                                    <pre>@Content</pre>
                                }
                                
                                @if (!string.IsNullOrWhiteSpace(StructuredDataParseError))
                                {
                                    <MudAlert Severity="Severity.Warning" Dense="true" Class="mt-3">
                                        <strong>结构化数据解析错误：</strong> @StructuredDataParseError
                                    </MudAlert>
                                }
                            </MudPaper>
                        </MudTabPanel>
                        
                        @if (HasStructuredData && OutputFields?.Any() == true && !string.IsNullOrEmpty(OriginalPrompt))
                        {
                            <MudTabPanel Text="调试信息" Icon="@Icons.Material.Filled.BugReport">
                                <MudPaper Class="pa-4 result-content" Outlined="true">
                                    <MudExpansionPanels Elevation="0" Dense="true">
                                        <MudExpansionPanel Title="原始 Prompt">
                                            <TitleContent>
                                                <div style="display: flex; align-items: center;">
                                                    <MudIcon Icon="@Icons.Material.Filled.TextSnippet" Class="mr-3" />
                                                    <MudText><strong>原始 Prompt</strong></MudText>
                                                </div>
                                            </TitleContent>
                                            <ChildContent>
                                                <pre style="background: #f8f9fa; padding: 12px; border-radius: 4px; margin: 0;">@OriginalPrompt</pre>
                                            </ChildContent>
                                        </MudExpansionPanel>
                                        
                                        <MudExpansionPanel Title="增强后的 Prompt">
                                            <TitleContent>
                                                <div style="display: flex; align-items: center;">
                                                    <MudIcon Icon="@Icons.Material.Filled.AutoFixHigh" Class="mr-3" />
                                                    <MudText><strong>增强后的 Prompt</strong></MudText>
                                                    <MudSpacer />
                                                    <MudChip T="string" Size="Size.Small" Color="Color.Info">
                                                        @(GetEnhancedPrompt().Length - OriginalPrompt.Length) 字符增强
                                                    </MudChip>
                                                </div>
                                            </TitleContent>
                                            <ChildContent>
                                                <pre style="background: #f0f7ff; padding: 12px; border-radius: 4px; margin: 0;">@GetEnhancedPrompt()</pre>
                                            </ChildContent>
                                        </MudExpansionPanel>
                                        
                                        <MudExpansionPanel Title="字段定义">
                                            <TitleContent>
                                                <div style="display: flex; align-items: center;">
                                                    <MudIcon Icon="@Icons.Material.Filled.Schema" Class="mr-3" />
                                                    <MudText><strong>字段定义</strong></MudText>
                                                    <MudSpacer />
                                                    <MudChip T="string" Size="Size.Small" Color="Color.Success">
                                                        @OutputFields.Count 个字段
                                                    </MudChip>
                                                </div>
                                            </TitleContent>
                                            <ChildContent>
                                                <MudSimpleTable Dense="true" Bordered="true">
                                                    <thead>
                                                        <tr>
                                                            <th style="font-weight: bold;">字段名</th>
                                                            <th style="font-weight: bold;">类型</th>
                                                            <th style="font-weight: bold;">必填</th>
                                                            <th style="font-weight: bold;">说明</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        @foreach (var field in OutputFields)
                                                        {
                                                            <tr>
                                                                <td style="font-family: monospace; font-weight: bold;">@field.Name</td>
                                                                <td>
                                                                    <MudChip T="string" Size="Size.Small" Color="Color.Secondary">@field.Type.ToString()</MudChip>
                                                                </td>
                                                                <td>
                                                                    @if (field.IsRequired)
                                                                    {
                                                                        <MudChip T="string" Size="Size.Small" Color="Color.Error">必填</MudChip>
                                                                    }
                                                                    else
                                                                    {
                                                                        <MudChip T="string" Size="Size.Small" Color="Color.Default">可选</MudChip>
                                                                    }
                                                                </td>
                                                                <td>@(field.Comment ?? "-")</td>
                                                            </tr>
                                                        }
                                                    </tbody>
                                                </MudSimpleTable>
                                            </ChildContent>
                                        </MudExpansionPanel>
                                    </MudExpansionPanels>
                                </MudPaper>
                            </MudTabPanel>
                        }
                    </MudTabs>
                </div>

                <!-- 统计信息 -->
                @if (Cost.HasValue || GeneratedAt.HasValue)
                {
                    <div>
                        <MudText Typo="Typo.subtitle2" Class="mb-2">统计信息</MudText>
                        <MudGrid Spacing="2">
                            @if (Cost.HasValue)
                            {
                                <MudItem xs="6">
                                    <div class="d-flex align-center">
                                        <MudIcon Icon="@Icons.Material.Filled.AttachMoney" Color="Color.Success" Size="Size.Small" Class="mr-1" />
                                        <MudText Typo="Typo.body2">成本: $@Cost.Value.ToString("F3")</MudText>
                                    </div>
                                </MudItem>
                            }
                            @if (GeneratedAt.HasValue)
                            {
                                <MudItem xs="6">
                                    <div class="d-flex align-center">
                                        <MudIcon Icon="@Icons.Material.Filled.Schedule" Color="Color.Info" Size="Size.Small" Class="mr-1" />
                                        <MudText Typo="Typo.body2">生成时间: @GeneratedAt.Value.ToString("MM-dd HH:mm")</MudText>
                                    </div>
                                </MudItem>
                            }
                        </MudGrid>
                    </div>
                }
            </MudStack>
        </MudContainer>
    </DialogContent>
    
    <DialogActions>
        <MudButton OnClick="Cancel">关闭</MudButton>
        <MudButton Color="Color.Primary" 
                  Variant="Variant.Filled" 
                  StartIcon="@Icons.Material.Filled.ContentCopy"
                  OnClick="CopyContent">
            复制内容
        </MudButton>
    </DialogActions>
</MudDialog>

<style>
.combination-id {
    font-family: 'Courier New', monospace;
    padding: 2px 6px;
    border-radius: 4px;
    font-size: 0.75rem;
}

.result-content {
    background-color: #f8f9fa;
}

.result-text {
    font-style: italic;
    line-height: 1.6;
}
</style>

@code {
    [CascadingParameter] IMudDialogInstance MudDialog { get; set; } = null!;
    
    [Parameter] public string Content { get; set; } = string.Empty;
    [Parameter] public string CombinationId { get; set; } = string.Empty;
    [Parameter] public Dictionary<string, object>? InputData { get; set; }
    [Parameter] public decimal? Cost { get; set; }
    [Parameter] public DateTime? GeneratedAt { get; set; }
    
    /// <summary>
    /// 是否包含结构化数据
    /// </summary>
    [Parameter] public bool HasStructuredData { get; set; } = false;
    
    /// <summary>
    /// 结构化数据字段
    /// </summary>
    [Parameter] public Dictionary<string, object?>? StructuredFields { get; set; }
    
    /// <summary>
    /// 结构化数据解析错误（如果有）
    /// </summary>
    [Parameter] public string? StructuredDataParseError { get; set; }
    
    /// <summary>
    /// 输出字段定义（用于调试）
    /// </summary>
    [Parameter] public List<ContentEngine.Core.DataPipeline.Models.FieldDefinition>? OutputFields { get; set; }
    
    /// <summary>
    /// 原始Prompt（用于调试）
    /// </summary>
    [Parameter] public string? OriginalPrompt { get; set; }

    private async Task CopyContent()
    {
        try
        {
            await JSRuntime.InvokeVoidAsync("navigator.clipboard.writeText", Content);
            Snackbar.Add("内容已复制到剪贴板", Severity.Success);
        }
        catch
        {
            Snackbar.Add("复制失败，请手动选择并复制", Severity.Warning);
        }
    }

    private void Cancel()
    {
        MudDialog.Cancel();
    }

    private bool TryFormatJson(string? text, out string pretty)
    {
        pretty = string.Empty;
        if (string.IsNullOrWhiteSpace(text)) return false;
        try
        {
            using var doc = System.Text.Json.JsonDocument.Parse(text);
            var opts = new System.Text.Json.JsonSerializerOptions { WriteIndented = true };
            pretty = System.Text.Json.JsonSerializer.Serialize(doc.RootElement, opts);
            return true;
        }
        catch
        {
            return false;
        }
    }

    private string GetEnhancedPrompt()
    {
        if (string.IsNullOrEmpty(OriginalPrompt) || OutputFields?.Any() != true)
            return OriginalPrompt ?? string.Empty;
        
        return ContentEngine.Core.Utils.JsonParsingUtils.BuildEnhancedPrompt(OriginalPrompt, OutputFields);
    }
} 