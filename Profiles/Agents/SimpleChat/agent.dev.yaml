# Agents/SimpleChat/agent.dev.yaml
# Environment-specific overrides for 'dev'

# Example: Use a different model ID during development
models:
  chat: gemini-2.5-flash # Maybe a cheaper/faster dev deployment

# Example: Maybe disable a plugin during development
# plugins:
#  - ./Plugins/ChatHelper 

# Example: Increase log level for debugging in dev
logLevel: Debug 