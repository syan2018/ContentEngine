description: "Suggests schema fields based on natural language description."

models:
  # 使用你在 models.yaml 中定义好的聊天模型 ID
  chat: gpt-5-nano

plugins:
  # 引用 Agent 本地的插件
  - SchemaHelper
  - DataStructuring
